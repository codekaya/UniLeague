<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="/js/university_page.js"></script>
    
    <title>Document</title>
</head>
<body>
    <h1><%= uni_info.title %></h1>
    <h2><%= uni_info.Region %></h2>
    <h2><%= uni_info.Province %></h2>

    <% comment_info.forEach(comment => { %>
      <p id="<%= comment.id %>"><%= comment.content %> || <label id="like<%= comment.id %>"> <%= comment.likes.length %> </label> || <label id="dislike<%= comment.id %>"> <%= comment.dislikes.length %> </label>
        <button onclick="likeComment('<%= comment.id %>')">Like</button>
        <button onclick="dislikeComment('<%= comment.id %>')">Dislike</button>
        <button onclick="deleteComment('<%= comment.id %>')">Delete</button>
      </p>
    <% }) %>

<%=  %>


    <h2>Kullanıcı Adı ve Metin Formu</h2>

    <label for="username">Kullanıcı Adı:</label>
    <input type="text" id="username" name="username" required><br><br>

    <label for="content">Metin:</label><br>
    <textarea id="content" name="content" rows="4" cols="50" required></textarea><br><br>

    <button onclick="addComment('<%= uni_info._id %>')">Gönder</button>
  
  <h2>Ratings</h2>
  <form id="ratingForm" method="post">
    <p>
        <%= (isNaN(uni_info.edu_point / uni_info.rate_count) ? 0 : (uni_info.edu_point / uni_info.rate_count).toFixed(1)) %> ||
        <input type="number" id="edu_point" min="1" max="10" required>
    </p>
    <p>
        <%= (isNaN(uni_info.dorm_point / uni_info.rate_count) ? 0 : (uni_info.dorm_point / uni_info.rate_count).toFixed(1)) %> ||
        <input type="number" id="dorm_point" min="1" max="10" required>
    </p>
    <p>
        <%= (isNaN(uni_info.trans_point / uni_info.rate_count) ? 0 : (uni_info.trans_point / uni_info.rate_count).toFixed(1)) %> ||
        <input type="number" id="trans_point" min="1" max="10" required>
    </p>
    <p>
        <%= (isNaN(uni_info.edu_point / uni_info.rate_count) ? 0 : (uni_info.edu_point / uni_info.rate_count).toFixed(1)) %> ||
        <input type="number" id="campus_point" min="1" max="10" required>
    </p>
</form>
  <button onclick="giveRating('<%= uni_info._id %>')">Gönder</button>
  
</body>
</html>